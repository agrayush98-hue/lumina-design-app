/**
 * Vercel serverless function — sends email via Zoho SMTP.
 * Replaces the former Resend-based implementation.
 * See zoho-send-email.js for implementation details.
 */

export { default } from './zoho-send-email.js'
