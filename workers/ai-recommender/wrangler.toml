name = "lumina-ai-recommender"
main = "index.js"
compatibility_date = "2024-01-01"

# Deploy: wrangler deploy
# Set secret: wrangler secret put ANTHROPIC_API_KEY
# (paste the key when prompted — it is stored encrypted, never in code)

[env.production]
name = "lumina-ai-recommender"
